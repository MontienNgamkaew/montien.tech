# Optional overrides. Defaults assume this app is served from /pnp-lessonplan/dist/
# inside the main pnp-portal project.
VITE_PORTAL_URL=../../
VITE_PORTAL_VERIFY_URL=../../api/verify.php

# Required only when using ThaiLLM proxy, training mode, or admin key pool.
VITE_THAILLM_PROXY_URL=
